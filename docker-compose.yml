version: '3.8'

networks:
  openhim:
    external: true

services:
  dhis2-pass-through-mediator:
    build: .
    name: dhis2-pass-through-mediator
    ports:
      - 3000:3000
    env_file:
      - .env