import { DEFAULT_PORT } from './defaults';
import {
    API_GLOBAL_BASE_URL,
    BASE_URL,
    MAX_REDIRECTS,
    PASSWORD,
    PORT,
    TIMEOUT,
    USERNAME,
} from './environment';

export {
    API_GLOBAL_BASE_URL,
    BASE_URL,
    MAX_REDIRECTS,
    PASSWORD,
    PORT,
    TIMEOUT,
    USERNAME,
    DEFAULT_PORT,
};
